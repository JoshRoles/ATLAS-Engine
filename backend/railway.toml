# Railway: set the service root directory to `backend` in the dashboard.
# Add a volume for ./data if you want SQLite to persist across deploys.

[build]
builder = "nixpacks"

[deploy]
startCommand = "uvicorn main:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "always"
